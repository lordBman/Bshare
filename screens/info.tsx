import { View } from "react-native";
import { RootStackScreenProps } from "../utils/types";

type InfoProps = RootStackScreenProps<"info">;

const Info: React.FC<InfoProps> = ({ navigation, route }) =>{
    return (
        <View>

        </View>
    );
}

export default Info;